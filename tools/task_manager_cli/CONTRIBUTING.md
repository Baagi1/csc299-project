# Contributing

Small project — please follow these rules when contributing:

- Write tests for new behavior (pytest preferred).
- Keep changes small and focused; reference the Speckit constitution in PRs.
- Run `python -m task_manager_cli.cli` commands during development to smoke-test.
